{
  "name": "racfit-site",
  "private": true,
  "description": "goracfit.com - static site on Cloudflare Workers with static assets",
  "scripts": {
    "build": "python3 build.py && npx --yes wrangler@4 pages functions build --outdir=./dist/worker/",
    "check": "python3 check-forms.py",
    "deploy": "npx --yes wrangler@4 deploy",
    "dev": "npx --yes wrangler@4 dev"
  }
}
